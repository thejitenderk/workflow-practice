name: Greetings

on:
  workflow_dispatch: 

env:
  NAME: 'jitender'

jobs:

        
  linuxgreeting:
    name: reply by linux $name

    runs-on: ubuntu-slim
    steps:
      - name: Hello Audience 
        run: echo "Hello Guys What's Up, I'm good $NAME"
  
