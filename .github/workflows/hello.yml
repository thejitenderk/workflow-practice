name: Infra Setup

on:
  workflow_dispatch: 
  pull_request: 
    branches:
      - main
  push:
    branches:
      - main
  

# user define
# pre-define variable
# run time variable

jobs:
  terraform_init:
    name: Terraform Init
    runs-on: ubuntu-latest
    steps:
      - name: Init
        run: terraform init
