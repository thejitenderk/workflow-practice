name: Hello Workflow

#none or manual trigger
on:
  workflow_dispatch:
    inputs:
      infra_env:
        description: 'Bhai kha p deploy karna h'
        type: string
        default: dev
      version:
       default: 1
       type: number
        




jobs:
  my_first_job:
    name: My first job
    runs-on: ubuntu-slim
    steps:
      - name: Greeting
        run: echo "ye pipeline ${{ github.event.inputs.infra_env}} me chalegi by ${{ vars.NAME }}- ${{ RUNNER.OS }} "
        
   

 
